import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  table: {
    minWidth: 650,
    marginBottom: 20,
  },
  title: {
    color: '#007bda',
    textAlign: 'center',
    marginBottom: 20,
  },
});

export default useStyles;
